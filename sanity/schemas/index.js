import { product } from './product';
import { category } from './category';
import { brand } from './brand';
import { enquiry } from './enquiry';

export const schemaTypes = [product, category, brand, enquiry];
